spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306?serverTimezone=Asia/Shanghai&Unicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
    cache: false

  messages:
    basename: i18n/login